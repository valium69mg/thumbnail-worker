# thumbnail-worker

# Pipeline Jenkins plugins

Para que funcione el Jenkinsfile se debe tener instalado el plugin de Docker Pipeline en Jenkins

# Instalar SonarScanner

En Jenkins → Manage Jenkins → Tools → SonarScanner installations → Add → ponle nombre sonar-scanner → marca "Install automatically".
